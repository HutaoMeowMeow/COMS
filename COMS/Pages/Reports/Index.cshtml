@page
@model COMS.Pages.Reports.IndexModel
@{
    ViewData["Title"] = "Community Reports";
}

<<<<<<< HEAD
<div class="page-header">
    <div>
        <h2>Community Reports</h2>
        <p class="text-muted mb-0">View and manage obstruction reports from residents.</p>
    </div>
    <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createReportModal">
        <i class="bi bi-plus-circle"></i> Submit Report
    </button>
=======
<div class="row mb-4">
    <div class="col-12">
        <div class="section-header">
            <div>
                <h2 class="page-title mb-0">Community Reports</h2>
                <p class="page-subtitle mb-0">Review and manage community-submitted canal reports.</p>
            </div>
            <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createReportModal">
                <i class="bi bi-plus-circle"></i> Submit Report
            </button>
        </div>
    </div>
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
<<<<<<< HEAD
            <table class="table table-hover" id="reportsTable">
=======
            <table class="table table-striped" id="reportsTable">
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Canal</th>
                        <th>Reported By</th>
                        <th>Type</th>
                        <th>Status</th>
                        <th>Reported At</th>
<<<<<<< HEAD
                        <th>Actions</th>
=======
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
                    </tr>
                </thead>
                <tbody id="reportsBody">
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade" id="createReportModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Submit Community Report</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="createReportForm">
                    <div class="mb-3">
                        <label class="form-label">Canal</label>
                        <select class="form-select" id="reportCanalId" required>
                            <option value="">Select Canal</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Report Type</label>
                        <select class="form-select" id="reportType">
                            <option value="Obstruction">Obstruction</option>
                            <option value="Debris">Debris</option>
                            <option value="Flooding">Flooding</option>
                            <option value="Other">Other</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Title</label>
<<<<<<< HEAD
                        <input type="text" class="form-control" id="reportTitle" placeholder="Brief description of the issue" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Description</label>
                        <textarea class="form-control" id="reportDescription" rows="3" placeholder="Provide details about the obstruction..." required></textarea>
=======
                        <input type="text" class="form-control" id="reportTitle" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Description</label>
                        <textarea class="form-control" id="reportDescription" rows="3" required></textarea>
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary" onclick="createReport()">Submit Report</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
async function loadCanals() {
    const response = await apiCall('/api/Canals');
    if (response.ok) {
        const canals = await response.json();
        const select = document.getElementById('reportCanalId');
<<<<<<< HEAD
        select.innerHTML = '<option value=\"\">Select Canal</option>' + canals.map(c => `<option value=\"${c.id}\">${c.name}</option>`).join('');
=======
        select.innerHTML = '<option value="">Select Canal</option>' + canals.map(c => `<option value="${c.id}">${c.name}</option>`).join('');
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
    }
}

async function loadReports() {
<<<<<<< HEAD
    const response = await apiCall('/api/Reports');
    if (response.ok) {
        const reports = await response.json();
        if (reports.length === 0) {
            document.getElementById('reportsBody').innerHTML = '<tr><td colspan="7" class="text-center text-muted">No reports found.</td></tr>';
            return;
        }
        document.getElementById('reportsBody').innerHTML = reports.map(r => `
            <tr>
                <td><strong>${r.title}</strong></td>
                <td>${r.canalName}</td>
                <td>${r.reportedByUserName}</td>
                <td>${r.reportType}</td>
                <td><span class="badge bg-${r.status === 'Pending' ? 'warning' : r.status === 'Verified' ? 'info' : r.status === 'Completed' ? 'success' : 'secondary'}">${r.status}</span></td>
                <td>${new Date(r.reportedAt).toLocaleString()}</td>
                <td>
                    <button class="btn btn-sm btn-info" onclick="viewReport('${r.id}')">View</button>
                </td>
            </tr>
        `).join('');
    }
}

function viewReport(id) {
    window.location.href = `/Reports/Details?id=${id}`;
}

=======
    showLoading();
    try {
        const response = await apiCall('/api/Reports');
        if (response.ok) {
            const reports = await response.json();
            const tbody = document.getElementById('reportsBody');
            if (reports.length === 0) {
                tbody.innerHTML = '<tr><td colspan="6" class="text-center text-muted">No reports found.</td></tr>';
            } else {
                tbody.innerHTML = reports.map(r => `
                    <tr>
                        <td>${r.title}</td>
                        <td>${r.canalName}</td>
                        <td>${r.reportedByUserName}</td>
                        <td>${r.reportType}</td>
                        <td><span class="badge bg-${r.status === 'Pending' ? 'warning' : r.status === 'Verified' ? 'info' : 'success'}">${r.status}</span></td>
                        <td>${new Date(r.reportedAt).toLocaleString()}</td>
                    </tr>
                `).join('');
            }
        }
    } catch (e) {
        showAlert('Failed to load reports: ' + e.message, 'danger');
    } finally {
        hideLoading();
    }
}

>>>>>>> db46004de7d488abfb831db9c6cd307518689719
async function createReport() {
    const data = {
        canalId: document.getElementById('reportCanalId').value,
        reportType: document.getElementById('reportType').value,
        title: document.getElementById('reportTitle').value,
        description: document.getElementById('reportDescription').value
    };

    const response = await apiCall('/api/Reports', {
        method: 'POST',
        body: JSON.stringify(data)
    });

    if (response.ok) {
        bootstrap.Modal.getInstance(document.getElementById('createReportModal')).hide();
        document.getElementById('createReportForm').reset();
        loadReports();
        showAlert('Report submitted successfully', 'success');
    } else {
        showAlert('Failed to submit report', 'danger');
    }
}

document.addEventListener('DOMContentLoaded', () => { loadCanals(); loadReports(); });
</script>
}
