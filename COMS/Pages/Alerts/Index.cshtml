@page
@model COMS.Pages.Alerts.IndexModel
@{
<<<<<<< HEAD
    ViewData["Title"] = "Obstruction Alerts";
}

<div class="page-header">
    <div>
        <h2>Obstruction Alerts</h2>
        <p class="text-muted mb-0">Monitor and manage canal obstruction alerts in real-time.</p>
    </div>
    <div class="btn-group" role="group">
        <button class="btn btn-outline-primary" onclick="loadAlerts('all')">All</button>
        <button class="btn btn-outline-warning" onclick="loadAlerts('active')">Active</button>
        <button class="btn btn-outline-success" onclick="loadAlerts('resolved')">Resolved</button>
=======
    ViewData["Title"] = "Alerts";
}

<div class="row mb-4">
    <div class="col-12">
        <div class="section-header">
            <div>
                <h2 class="page-title mb-0">Obstruction Alerts</h2>
                <p class="page-subtitle mb-0">Track and resolve canal obstruction alerts.</p>
            </div>
            <div>
                <button class="btn btn-outline-primary" onclick="loadAlerts('all')">All</button>
                <button class="btn btn-outline-warning" onclick="loadAlerts('active')">Active</button>
                <button class="btn btn-outline-success" onclick="loadAlerts('resolved')">Resolved</button>
            </div>
        </div>
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
    </div>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
<<<<<<< HEAD
            <table class="table table-hover" id="alertsTable">
=======
            <table class="table table-striped" id="alertsTable">
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Canal</th>
                        <th>Type</th>
                        <th>Severity</th>
                        <th>Status</th>
                        <th>Triggered At</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody id="alertsBody">
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade" id="updateAlertModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Update Alert</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="updateAlertForm">
                    <input type="hidden" id="alertId" />
                    <div class="mb-3">
                        <label class="form-label">Status</label>
                        <select class="form-select" id="alertStatus">
                            <option value="Active">Active</option>
                            <option value="Resolved">Resolved</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Resolution Notes</label>
                        <textarea class="form-control" id="alertNotes" rows="3"></textarea>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary" onclick="updateAlert()">Update</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
async function loadAlerts(filter = 'all') {
<<<<<<< HEAD
    let url = '/api/Alerts';
    if (filter === 'active') url = '/api/Alerts/active';
    if (filter === 'resolved') url = '/api/Alerts?status=Resolved';

    const response = await apiCall(url);
    if (response.ok) {
        const alerts = await response.json();
        if (alerts.length === 0) {
            document.getElementById('alertsBody').innerHTML = '<tr><td colspan="7" class="text-center text-muted">No alerts found.</td></tr>';
            return;
        }
        document.getElementById('alertsBody').innerHTML = alerts.map(a => `
            <tr>
                <td><strong>${a.title}</strong></td>
                <td>${a.canalName}</td>
                <td>${a.alertType}</td>
                <td><span class="badge bg-${a.severity === 'High' ? 'danger' : 'warning'}">${a.severity}</span></td>
                <td><span class="badge bg-${a.status === 'Active' ? 'danger' : 'success'}">${a.status}</span></td>
                <td>${new Date(a.triggeredAt).toLocaleString()}</td>
                <td>
                    <button class="btn btn-sm btn-primary" onclick="openUpdateModal('${a.id}', '${a.status}')">Update</button>
                </td>
            </tr>
        `).join('');
=======
    showLoading();
    try {
        let url = '/api/Alerts';
        if (filter === 'active') url = '/api/Alerts/active';
        if (filter === 'resolved') url = '/api/Alerts?status=Resolved';

        const response = await apiCall(url);
        if (response.ok) {
            const alerts = await response.json();
            const tbody = document.getElementById('alertsBody');
            if (alerts.length === 0) {
                tbody.innerHTML = '<tr><td colspan="7" class="text-center text-muted">No alerts found.</td></tr>';
            } else {
                tbody.innerHTML = alerts.map(a => `
                    <tr>
                        <td>${a.title}</td>
                        <td>${a.canalName}</td>
                        <td>${a.alertType}</td>
                        <td><span class="badge bg-${a.severity === 'High' ? 'danger' : 'warning'}">${a.severity}</span></td>
                        <td><span class="badge bg-${a.status === 'Active' ? 'danger' : 'success'}">${a.status}</span></td>
                        <td>${new Date(a.triggeredAt).toLocaleString()}</td>
                        <td>
                            <button class="btn btn-sm btn-primary" onclick="openUpdateModal('${a.id}', '${a.status}')">Update</button>
                        </td>
                    </tr>
                `).join('');
            }
        }
    } catch (e) {
        showAlert('Failed to load alerts: ' + e.message, 'danger');
    } finally {
        hideLoading();
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
    }
}

function openUpdateModal(id, status) {
    document.getElementById('alertId').value = id;
    document.getElementById('alertStatus').value = status;
    new bootstrap.Modal(document.getElementById('updateAlertModal')).show();
}

async function updateAlert() {
    const id = document.getElementById('alertId').value;
    const status = document.getElementById('alertStatus').value;
    const notes = document.getElementById('alertNotes').value;

    const response = await apiCall(`/api/Alerts/${id}`, {
        method: 'PUT',
        body: JSON.stringify({ status, resolutionNotes: notes || undefined })
    });

    if (response.ok) {
        bootstrap.Modal.getInstance(document.getElementById('updateAlertModal')).hide();
        loadAlerts();
<<<<<<< HEAD
        showAlert('Alert updated successfully', 'success');
=======
        showAlert('Alert updated', 'success');
>>>>>>> db46004de7d488abfb831db9c6cd307518689719
    } else {
        showAlert('Failed to update alert', 'danger');
    }
}

document.addEventListener('DOMContentLoaded', () => loadAlerts());
initSignalR((type, data) => { if (type === 'alert') loadAlerts(); });
</script>
}
